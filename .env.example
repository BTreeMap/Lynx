# Database Configuration
# Options: sqlite, postgres
DATABASE_BACKEND=sqlite
# For SQLite
DATABASE_URL=sqlite://./lynx.db
# For PostgreSQL (example)
# DATABASE_URL=postgresql://user:password@localhost/lynx

# API Server Configuration (for management operations)
API_HOST=127.0.0.1
API_PORT=8080

# Redirect Server Configuration (for client-facing URL redirects)
REDIRECT_HOST=127.0.0.1
REDIRECT_PORT=3000

# Authentication Configuration
# Set to 'true' to completely disable authentication (useful when control plane is in secure environment)
# DISABLE_AUTH=false

# Comma-separated list of API keys (leave empty for development mode)
# Example: API_KEYS=key1,key2,key3
API_KEYS=
